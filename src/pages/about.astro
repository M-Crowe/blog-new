---
import BaseLayout from '../layouts/BaseLayout.astro';
import { siteConfig } from '../lib/config';
---

<BaseLayout
  title="关于"
  description={`关于博主 ${siteConfig.author.name} 与个人博客的构建理念。`}
  currentPath="/about"
>
  <main class="main-content">
    <h1 class="page-title">关于我</h1>

    <section class="about-card">
      <div class="avatar">
        <img
          src={siteConfig.author.avatar}
          alt={siteConfig.author.name}
          loading="eager"
          decoding="async"
        />
      </div>
      <div class="card-info">
        <h2>{siteConfig.author.name}</h2>
        <p>{siteConfig.author.bio}</p>
      </div>
    </section>

    <section class="about-content">
      <p>你好，我是 {siteConfig.author.name}。</p>
      <p>
        欢迎来到我的个人博客。这里主要是我记录生活见闻与技术探索的自留地。在这个充满噪音的时代，我希望能通过文字与图像保留一些有价值的思考与痕迹。
      </p>

      <h2>网站理念</h2>
      <p>
        本站基于 <strong>Astro</strong> 与 <strong>Pretext</strong> 构建。崇尚极简主义的排版流，探索 Canvas
        文本测量与现代 Web 排版工程的边界，只为呈现最纯粹的阅读体验。
      </p>

      <h2>联系方式</h2>
      <p>如果你发现文章有误，或者对博客的构建方式与摄影作品感兴趣，欢迎通过以下方式与我交流探讨：</p>
      <p>
        Email: <a
          href={`mailto:${siteConfig.author.email}`}
          class="contact-link"
        >
          {siteConfig.author.email}
        </a>
      </p>
    </section>
  </main>
</BaseLayout>

<style>
  /* 居中阅读区域 */
  .main-content {
    max-width: 800px;
    margin: 40px auto 60px;
    line-height: 1.8;
  }

  .page-title {
    font-size: 2rem;
    font-weight: 600;
    margin-bottom: 40px;
    color: var(--text-main);
  }

  .about-content {
    margin-bottom: 60px;
  }

  .about-content h2 {
    font-size: 1.35rem;
    margin: 40px 0 20px;
    font-weight: 600;
    border-bottom: 1px solid var(--border-color);
    padding-bottom: 10px;
  }

  .about-content p {
    margin-bottom: 20px;
    font-size: 1.05rem;
    color: var(--text-main);
  }

  .about-card {
    background: rgba(0, 0, 0, 0.02);
    border: 1px solid var(--border-color);
    border-radius: var(--radius);
    padding: 32px;
    margin: 40px 0;
    display: flex;
    align-items: center;
    gap: 24px;
  }

  .avatar {
    width: 80px;
    height: 80px;
    border-radius: 50%;
    background: #e0e0e0;
    flex-shrink: 0;
    overflow: hidden;
  }

  .avatar img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .card-info h2 {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 8px;
    border: none;
    padding: 0;
  }

  .card-info p {
    margin: 0;
    font-size: 0.95rem;
    color: var(--text-muted);
    line-height: 1.5;
  }

  .contact-link {
    text-decoration: underline;
    color: var(--primary-accent);
    text-underline-offset: 4px;
    transition: opacity 0.2s ease;
  }

  .contact-link:hover {
    opacity: 0.8;
  }

  @media (max-width: 768px) {
    .about-card {
      flex-direction: column;
      text-align: center;
      padding: 24px;
    }
  }
</style>